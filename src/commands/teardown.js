import chalk from 'chalk';
export async function teardown(opts) {
  console.log(chalk.yellow('teardown: coming in v0.2 — track progress at https://github.com/gethookd-labs/gethookd-agent/issues/1'));
}
